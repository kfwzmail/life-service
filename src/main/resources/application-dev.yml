# ==================== 开发环境配置 ====================
spring:
  application:
    name: life-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/aimarket?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: aimarket
    password: Wjw@6899
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.std.lifeService.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: ${JWT_SECRET:YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY=}
  expiration: 86400000

server:
  port: 8080

logging:
  level:
    com.std.lifeService: DEBUG
